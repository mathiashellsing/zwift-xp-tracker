# Claude Instructions for zwift-xp-tracker

## Git Workflow

- After committing and pushing changes, always create a pull request targeting `main` unless the user explicitly says not to.
- Develop on the designated feature branch (check session context for branch name).
- Never push directly to `main`.
